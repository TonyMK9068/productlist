= (@user.first_name || @user.username || @user.eamil) + " has shared their list," + @list.title + ", with you!"

Click the link below to view it at GiftShare!

= link_to @list.title, list_path(@list)
